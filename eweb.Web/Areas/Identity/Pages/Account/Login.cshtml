@page
@model LoginModel

@{
    ViewData["Title"] = "Вхід";
}

<div class="auth-page">
    <div class="auth-header">
        <div class="page-kicker">Акаунт</div>
        <h1>Вхід</h1>
        <p>Увійдіть, щоб продовжити навчання.</p>
    </div>

    <section class="auth-card">
        <form id="account" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

            <div class="auth-field">
                <label asp-for="Input.Email">Email</label>
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="auth-field">
                <label asp-for="Input.Password">Пароль</label>
                <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="Введіть пароль" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="form-check auth-check">
                <input class="form-check-input" asp-for="Input.RememberMe" />
                <label asp-for="Input.RememberMe" class="form-check-label">
                    @Html.DisplayNameFor(m => m.Input.RememberMe)
                </label>
            </div>

            <button id="login-submit" type="submit" class="auth-submit">
                Увійти
            </button>

            <div class="auth-links">
                <a id="forgot-password" asp-page="./ForgotPassword">Забули пароль?</a>
                <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Створити акаунт</a>
                <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Повторно надіслати підтвердження</a>
            </div>
        </form>
    </section>

    @if ((Model.ExternalLogins?.Count ?? 0) > 0)
    {
        <section class="auth-card auth-card-secondary">
            <h2>Інші способи входу</h2>
            <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <div class="action-row">
                    @foreach (var provider in Model.ExternalLogins!)
                    {
                        <button type="submit"
                                class="btn btn-outline-primary"
                                name="provider"
                                value="@provider.Name"
                                title="Увійти через @provider.DisplayName">
                            @provider.DisplayName
                        </button>
                    }
                </div>
            </form>
        </section>
    }
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
